# Services

Service modules will hold business workflows that coordinate repositories, tools, AI orchestration, uploads, and analytics.

Keep HTTP route parsing in `server/app.mjs`, durable storage in `server/repositories`, and cross-domain business operations here.
