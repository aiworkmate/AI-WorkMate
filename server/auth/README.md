# Auth

Auth modules will hold the Supabase Auth adapter and any compatibility code needed while AI WorkMate transitions from local sessions to Supabase Auth.

Do not place service role keys or provider secrets in frontend code. Server-only auth helpers belong here.
